spring:
  application:
    name: mealdangapi

  datasource:
    url: jdbc:mysql://altaria.proxy.rlwy.net:23178/mealdang
    username: root
    password: ${DB_PASSWORD}

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000

kakao:
  client-id: ${KAKAO_CLIENT_ID}
  redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:8080/api/users/kakao/callback}
